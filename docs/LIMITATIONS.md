# Limitations

Docling/OCR, Qdrant, reranking, vision inspection, and NPU execution are optional extensions and are not silently substituted. Native PDF extraction reports image-only pages as requiring OCR. The deterministic embedding is a local baseline, not a claim of production embedding quality.

