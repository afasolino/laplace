# Linux/WSL installation

Run `bash scripts/bootstrap.sh`, then `research-workspace probe`. Keep Ollama/llama.cpp bound to `127.0.0.1`; install CUDA-enabled Torch separately only for an explicitly tested model runtime.

